<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_predefined_habit"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardUseCompatPadding="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Habit Icon -->
        <ImageView
            android:id="@+id/iv_habit_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_logo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Habit Title -->
        <TextView
            android:id="@+id/tv_habit_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Habit Title"
            android:textColor="@color/primary_text"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/btn_add_habit"
            app:layout_constraintStart_toEndOf="@id/iv_habit_icon"
            app:layout_constraintTop_toTopOf="@id/iv_habit_icon" />

        <!-- Habit Description -->
        <TextView
            android:id="@+id/tv_habit_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Habit description goes here"
            android:textColor="@color/secondary_text"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="@id/tv_habit_title"
            app:layout_constraintStart_toStartOf="@id/tv_habit_title"
            app:layout_constraintTop_toBottomOf="@id/tv_habit_title" />

        <!-- Habit Category -->
        <TextView
            android:id="@+id/tv_habit_category"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Category"
            android:textColor="@color/accent_color"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@id/tv_habit_title"
            app:layout_constraintTop_toBottomOf="@id/tv_habit_description" />

        <!-- Suggested Count -->
        <TextView
            android:id="@+id/tv_suggested_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="0 users"
            android:textColor="@color/tertiary_text"
            android:textSize="12sp"
            app:layout_constraintBaseline_toBaselineOf="@id/tv_habit_category"
            app:layout_constraintStart_toEndOf="@id/tv_habit_category" />

        <!-- Add Button -->
        <View
            android:id="@+id/btn_add_habit"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/ic_add_circle"
            android:padding="8dp"
            app:layout_constraintBottom_toBottomOf="@id/iv_habit_icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/iv_habit_icon" />
        <TextView
            android:id="@+id/tv_custom_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CUSTOM"
            android:textSize="10sp"
            android:textStyle="bold"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:background="@drawable/custom_badge_background"
            android:textColor="@android:color/white"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>